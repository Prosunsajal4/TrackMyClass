const express = require('express');
const router = express.Router();

// This route is handled by courseRoutes, keeping for structure
// All attendance operations are part of course routes

module.exports = router;
